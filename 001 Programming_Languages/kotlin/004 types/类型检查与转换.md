---
date: 2026-05-06
tags:
  - kotlin
---
在kotlin,我可以在运行时对类型做两件事，1时检查是否是特定类型，2是将其`转换`(convert)为另一类型



## 用 `is`   `!is`操作符来检查

使用操作符（或将其否定`nagate`）来检查对象在运行时是否匹配某个类型：`is !is`

```
fun main() {
    val input: Any = "Hello, Kotlin"

    if (input is String) {
        println("Message length: ${input.length}")
        // Message length: 13
    }

    if (input !is String) { // Same as !(input is String)
        println("Input is not a valid message")
    } else {
        println("Processing message: ${input.length} characters")
        // Processing message: 13 characters
    }
}
```

也可用于检测一个对象是否匹配 子类
```
fun handleAnimal(animal: Animal) {
    println("Handling animal: ${animal.name}")
    animal.speak()
    
if(animal is Dog){
	println("Special care instructions: This is a dog.")
} else if(animal is Cat){
	println("Special care instructions: This is a cat.")
}
```

本示例使用操作符检查类实例是否有子类型或打印相关的关怀指令。`is``Animal``Dog``Cat`
相关`relevant`  说明`instruction`

你可以检查一个对象是否是其声明类型的超类型，但这并不值得，因为答案总是成立的。每个类实例本身就是其超类型的实例。

---

## 类型转换

在某些情况下，编译器会自动转换类型， 这种是 智能转换
需要显式转换的时候 可以使用 操作符 `as? as `



## 1. 判断规则（底层三步走）

1. 先看**对象是不是 `null`**
    
    - 如果对象是 `null` → 直接返回 `false`（除非你判断可空类型）
    
2. 再拿**对象运行时真实 Class**
    
    - 不是你定义的变量类型，是 **new 出来的真实类型**
    
3. 判断：真实类型 是否 **等于 / 是子类 / 实现了接口** 目标类型
    
    满足任意一个 → `true`

## 智能转换
编译器跟踪类型检查和显式铸造以识别不可变值，并自动插入隐式（安全）铸造：
```
fun logMessage(data: Any) {
    // data is automatically cast to String
    if (data is String) {
        println("Received text: ${data.length} characters")
    }
}

fun main() {
    logMessage("Server started")
    // Received text: 14 characters
    logMessage(404)
}


```

编译器甚至足够聪明，知道如果负面检查导致返回，铸造是安全的：
```
fun logMessage(data: Any){
	if(data !is String)return
	println("Received text: ${data.length} characters")
}
fun main(){
	 logMessage("User signed in")
    // Received text: 14 characters
    logMessage(true)
}
```

```
收到的文本：14个字符
```




### 控制流程
智能投射不仅适用于条件表达式，也适用于`when`表达式
```
fun processInput(data:Any){
	when(data){
		 // data is automatically cast to Int
        is Int -> println("Log: Assigned new ID ${data + 1}")
        // data is automatically cast to String
        is String -> println("Log: Received message \"$data\"")
        // data is automatically cast to IntArray
        is IntArray -> println("Log: Processed scores, total = ${data.sum()}")
	}
}
fun main() {
    processInput(1001)
    // Log: Assigned new ID 1002
    processInput("System rebooted")
    // Log: Received message "System rebooted"
    processInput(intArrayOf(10, 20, 30))
    // Log: Processed scores, total = 60
}
```

while循环也可以
```
var robo=RobotAacuum(listOf("Living Room", "Kitchen", "Hallway"))
var status: Status = robo.status()
while(status is Ok){
	 println("Cleaning ${status.currentRoom}...")
    status = robo.clean()
}
// Cleaning Living Room...
// Finished cleaning Living Room
// Cleaning Kitchen...
// Finished cleaning Kitchen
// Cleaning Hallway...
// Finished cleaning Hallway

```

可以用于变量起个好名字
```
fun petAnimal(animal:Any){
	val isCat= animal is Cat
	if(isCat){
		 // The compiler can access information about
        // isCat, so it knows that animal was smart-cast
        // to the type Cat.
        // Therefore, the purr() function can be called.
        animal.purr()
	}
	fun main(){
    val kitty = Cat()
    petAnimal(kitty)
    // Purr purr
	
}
```

---

## 逻辑操作
如果左侧有类型检查（常规或负值），编译器可以在`&&`或 `||`的右侧执行智能转换：
相当于右侧那个也会按转换后的来求
```
// x is automatically cast to String on the right-hand side of `||`

 if (x !is String || x.length == 0) return 
 
 // x is automatically cast to String on the right-hand side of `&&`
 
if (x is String && x.length > 0) { print(x.length) 

// x is automatically cast to String }
```


对于多类型判断 kotlin会将其转化为多类型的共同父类

![](image/Pasted%20image%2020260506115420.png)


### 内联函数
